#' Soma Dois Numeros
#'
#' `somar()` retorna a soma de `x` e `y`.
#'
#' @param x Um numero
#' @param y Um numero
#' @return A soma de `x` e `y`
#' @examples
#' # Soma 1+1 = 2
#' somar(1, 1)
#' @export
somar <- function(x, y) {
  x + y
}
