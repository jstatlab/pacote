#' Soma Dois Numeros
#'
#' \code{somar()} retorna a soma de \code{x} e \code{y}.
#'
#' @param x Um numero
#' @param y Um numero
#' @return A soma de \code{x} e \code{y}
#' @examples
#' # Soma 1+1 = 2
#' somar(1, 1)
#' @export
somar <- function(x, y) {
  x + y
}
